# Design fixtures only

These are schema/conformance examples, NOT signed or installable artifacts. Repeated `1` digests are placeholders marked test-only. Bundle artifact byte length is illustrative and is not an integrity claim. Golden descriptor digests are computed from actual canonical fixture bytes. Full package integrity/signature tests are later runtime gates.
