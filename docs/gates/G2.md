# G2 — Lean engineering foundation

Status: foundation implemented; clean-checkout verification in progress. Formal EXIT remains blocked by owner-only controls below until evidence is returned or an explicit equivalent is approved.

## Scope

G1 baseline accepted for progression. Minimal pinned Node/npm toolchain; TypeScript/Prettier development tools; hash-pinned Python design-test dependencies. No production Kernel/SDK implementation, V1 imports, databases, containers or application services. The public package contains only a foundational data type.

## Delivered checks

Reproducible setup/lockfiles, build and type checks, formatting, Node unit/negative fixtures, retained G1 schema/design tests, AST import boundaries, safely planted synthetic-secret rejection, dependency/license checks. CI is SHA-pinned and contents:read only. Contribution/setup/testing/security-reporting and signed-candidate evidence-design documentation are included.

## Owner-only blockers

1. Effective SSH/root/password policy and firewall rules cannot be read by alica-dev. Partial SSH file includes PermitRootLogin yes; protected include prevents an effective-policy conclusion. Owner must provide the read-only inspection in `docs/operations/G2-HOST-SECURITY.md` and disposition the controls. No login/firewall changes were attempted.
2. No authenticated GitHub API session is available. SSH deploy access works but does not expose Actions status, billing/availability or branch-protection/ruleset enforcement. The workflow will be pushed, but a green run or required-check enforcement is NOT claimed. Owner must supply the run/required-check evidence, enable enforceable checks, or explicitly approve the documented clean-checkout local gate as an equivalent. None is pre-approved.

Record actual clean-checkout source, commands, exit codes, output/artifact hashes and known limitations in `evidence/g2/`. Local passing tests alone do not close these blockers. No Phase 3 work is authorized by this delivery.
