# Updated owner disposition

See [G2-OWNER-DISPOSITION](G2-OWNER-DISPOSITION.md): explicit owner approval now permits G3 under the manual gate. The original delivery record below is historical, not current status.

# G2 — Lean engineering foundation

Status: foundation implemented and clean-checkout verification PASSED. Formal EXIT remains blocked pending disposition of the now-reported host controls and verified CI enforcement or an explicit owner-approved equivalent.

## Scope

G1 baseline accepted for progression. Minimal pinned Node/npm toolchain; TypeScript/Prettier development tools; hash-pinned Python design-test dependencies. No production Kernel/SDK implementation, V1 imports, databases, containers or application services. The public package contains only a foundational data type.

## Delivered checks

Reproducible setup/lockfiles, build and type checks, formatting, Node unit/negative fixtures, retained G1 schema/design tests, AST import boundaries, safely planted synthetic-secret rejection, dependency/license checks. CI is SHA-pinned and contents:read only. Contribution/setup/testing/security-reporting and signed-candidate evidence-design documentation are included.

## Owner-only blockers

1. Owner-provided root output now confirms default effective PermitRootLogin yes, PasswordAuthentication yes, X11Forwarding yes, AllowTcpForwarding yes, and inactive UFW. See `evidence/g2/owner-host-inspection.json`. This closes the initial information gap, not the security control: remediation or explicit owner-approved risk disposition is still required. Match-specific policy, other/provider firewall controls, tested owner key/recovery access, update execution and backup policy remain unverified. No login/firewall changes were attempted.
2. No authenticated GitHub API session is available. SSH deploy access works but does not expose Actions status, billing/availability or branch-protection/ruleset enforcement. The workflow has been pushed, but a green run or required-check enforcement is NOT claimed. Owner must supply the run/required-check evidence, enable enforceable checks, or explicitly approve the documented clean-checkout local gate as an equivalent. None is pre-approved.

Record actual clean-checkout source, commands, exit codes, output/artifact hashes and known limitations in `evidence/g2/`. Local passing tests alone do not close these blockers. No Phase 3 work is authorized by this delivery.

## Executed clean-checkout acceptance

Source: `b827cda24ae11901bb0afdd80e92f8fb81161c70`. A separate clone with an empty HOME and initial PATH=/usr/bin:/bin installed all tools from committed pins and ran `npm run check` successfully. 14 foundation tests and 193 design tests passed. Build/type/format/schema/import/secret/license checks passed. The generated JavaScript/declaration hashes matched the development build and a second clean rebuild. Git reported no tracked or unignored changes.

The snapshot is a same-host independent checkout, not an isolated VM or offline Cell qualification. Evidence contains exact commands, output, source/lock hashes and artifact hashes. The build/test inputs are unchanged by this evidence-only closure commit.

**G2 EXIT remains BLOCKED**, solely on the unresolved owner host controls and authenticated CI/required-check enforcement or explicit owner-approved equivalent listed above. Local verification is not mislabeled remote CI success.
