# G0 — Approved scope and pinned reference baseline

Status: PASS within the explicitly disclosed distribution-reference scope.

ENTRY: Owner authorized starting Step 1 after receiving the initiation report.

DELIVERABLE: authorization record, cryptographic reference lock and component authority inventory. References are pinned to an immutable Git commit; no V1 source is imported into the runtime.

EXIT evidence: all paths in `docs/references/v1-baseline.lock.json` were retrieved successfully over HTTPS and SHA-256 recorded. The inventory was derived from the retrieved manifest. Private component implementation, named human owners and rebuildability of all V1 images remain outside this scoped gate and UNVERIFIED. This is not the original broader claim of a complete V1 internal audit.

No application files, databases, services or protected V1 artifacts were modified. G1 begins with normative design; production runtime implementation waits for its approval boundary.
