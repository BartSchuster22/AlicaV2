# G0 — Reference baseline closure and owner acceptance

**Technical status: COMPLETE subject to passing the recorded checks. EXIT status: OWNER ACCEPTANCE PENDING for the precise reference-review boundary below.**

This supersedes the earlier premature PASS label without rewriting its history. The owner authorized Phase-0 completion; the agent does not invent an explicit acceptance message.

## ENTRY

Owner instructed Step-1 work and subsequently explicitly requested completion of Phase 0 with no runtime implementation. Scope/assessment work is authorized. G1 proposals already present remain unapproved.

## Deliverable checklist

- [x] Scope, non-goals, owner decision authority and approval status: `docs/decisions/G0-SCOPE-AND-INPUTS.md`.
- [x] Original plan and complete replacement directive preserved with origin/hash and normalization disclosures.
- [x] Pinned V1 commit, raw source hashes, observed retrieval times and review limitations.
- [x] Every manifest component inventoried with authority, contracts, provided/required capabilities, persistence interpretation, declared artifact/config/evidence bindings and unresolved owner/source facts.
- [x] Additional concept-document status and document contradictions explicitly recorded.
- [x] Protected V1 deployments/licensing evidence excluded from targets.
- [x] Reference index, existing assessment, inventory, approval/work-authorization record and initial ADR register linked.

## Executed checks

Run `python3 tests/reference/verify_g0.py --online`. Local verification checks owner-source hashes, manifest snapshot, inventory completeness, declared artifact bindings, reference metadata and file-boundary allowlist. Online verification independently re-retrieves each locked URL and rejects raw digest/length mismatch. SBOM/provenance raw digests are checked against the manifest. Actual results and input hashes are recorded in `evidence/g0/execution.json`.

No-runtime/data checking uses a strict repository file-category allowlist plus executable-source restriction to test harnesses; no V1 source modules, application datasets, runtime packages, symlinks/submodules or executable artifacts are allowed. This is a G0 repository boundary check, not a universal detector of secrets/data semantics or a full host audit.

## Precise boundary submitted for owner acceptance

The reference baseline covers public Alica-DSH distribution metadata/documents/scripts at the pinned commit. It enumerates all declared components and verifies selected source/evidence bytes. It does NOT verify private component code, source-to-binary builds, every OCI/executable artifact, complete operational backup ordering, named human ownership or the release signature chain. These remain explicitly UNVERIFIED; no production source/data is required for G0.

No additional concept document is available in the supplied inputs; existence elsewhere is UNKNOWN and acknowledged. No missing input is treated as an approved normative specification.

## EXIT

Technical work and checks are delivered; owner acceptance of this exact scope/boundary remains the gate-closing action. G0 is not silently marked PASS. G1 is not accepted or advanced by this closure work. No runtime implementation occurred.
