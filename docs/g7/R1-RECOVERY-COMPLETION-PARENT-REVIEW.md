# Explicit clean-abort recovery and dependency subassembly — parent review

Baseline a314e56a17958fa7befa0d07b90b8327c8e2fd0c. Bounded component qualification, NOT G7 acceptance or complete crash recovery.

Independent full npm run check exited 0: Node 170/170, 162/162 and 357/357; Python 193, 30, 11, 9 nested custody tests and 121 passed. Seven source/document hashes match before and after independent execution and current local files. Parent independently checked all 17,063 returned remote evidence hashes using the documented nested returned layout and seven final source/document hashes. Initial parent evidence check used the wrong flat location; corrected path lookup verified all entries, no evidence alteration. Independent lossless log: evidence/g7/recovery-parent/full-check.log.gz.

Reviewed Cell private clean-abort error identity, ownedMaintain guards and explicit ownedRecoverPrior flow, requirement-to-test mapping, execution and earlier failure dispositions, and dependency assembler. Actual cleanup/durable ABORTED and unchanged prior selection, not public error fields or persisted records, permit retaining continuously held authority. Current prior authority/bytes must be revalidated explicitly; serving is separately gated and does not automatically roll back or replay target mutation. Lost custody still requires fencing. Conditional successful detached target-crash reconciliation and full durability/host qualification remain unqualified; denial tests are not proof of successful recovery.

Dependency materialization is an exclusively created trusted single-writer build-time artifact, not a hardened untrusted extractor. It checks locked identity/dependency/license relationships and inventories actual staged bytes without executing package code during assembly. Isolated staged execution is exercised by tests. Lock tarball integrity metadata is provenance only, not independent tarball-byte verification. Native builds and all historical contract/admin/serving suites were rerun by the independent full check. Current result does not qualify complete runtime assembly, full SBOM or bootstrap.

Worker docs describe pre-publication handoff; this review records subsequent independent qualification. Raw historical evidence and warning/failure records are preserved without whitespace normalization. No production deployment/service/RAM/swap/signing or protected licensing/archive changes. Remaining work: full deterministic assembly/provenance/licenses/SBOM/bootstrap, encrypted stopped backup/restore, identical-byte online/disconnected two-host qualification, unresolved approved successful crash reconciliation, owner-controlled signing and final acceptance.
