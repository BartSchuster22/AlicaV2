# Parent execution review: Cell preparation only

The parent completed the scoped development transfer and built the native flock helper using the existing pinned toolchain. The worker handoff and original transfer/local-toolchain failures remain historical evidence, not current transfer blockers.

Actual execution uncovered missing `--experimental-vm-modules` flags in child test processes and incompatibility between null-prototype bounded-parser data and Ajv uniqueItems comparison. Child invocations now include the required flag; the schema validator receives an equivalent canonical JSON value with ordinary JSON prototypes. Schema constraints and authority checks were not weakened.

The corrected focused suite passed **24 tests, zero failures/skips** on the development host. It exercised private initialization and identity/floor continuity, actual staged transactions, cross-process flock contention and release after SIGKILL, crash readback at journal/floor boundaries, corruption/illegal-edge rejection, unsafe roots/links/modes and invalid trust/authorization rejection. Native build inputs and output hash are in the focused logs.

The initial full regression stopped at the import-boundary guard: Cell directly used Ajv without declaring that host-tool import. The correction explicitly pins the existing Ajv 8.20.0 in root development dependencies (root remains development-only), updates the lockfile offline with scripts disabled, and permits only `tools/g7-cell.mjs` -> `ajv/dist/2020.js`. No new package version was introduced. An adversarial foundation test verifies that other tools, arbitrary tests, consumer/SDK packages and other Ajv entry points remain denied. Boundary, dependency/license, foundation and all 24 preparation tests passed after the fix. Original failure and corrected logs are preserved. Standalone runtime packaging must separately include and inventory the actual runtime dependency; this development declaration is not a shipping manifest.

The corrected full regression passed (exit 0), followed by passing final formatting and diff checks. It includes 170 foundation/Kernel/contracts/SDK Node tests, 160 IPC tests, 128 combined G7 Node tests, and Python suites of 193 specification, 30 G6 model, 11 bootstrap and 119 G7 contract/model tests. All reported Node suites have zero failures. The 24 preparation tests are included in the combined G7 suite, not additional to it. `parent-tested-source.json` binds the full log and compares the exact changed source hashes with the development host. This is preparation-only evidence, not lifecycle qualification.

This is not a completed Cell lifecycle. There is no accepted-record publication, install/upgrade completion, runtime activation/readiness/reap, start/stop/admin protocol, backup/restore or standalone/two-host qualification. Those commands remain unavailable and their rejection is tested. No production key access/signing occurred.
