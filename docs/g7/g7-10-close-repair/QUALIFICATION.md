# G7-10 close repair: scoped qualification / rereview handoff
Status: TESTED CANDIDATE, NOT independently SOURCE_READY, not published.
One focused independent rereview is pending: no independent reviewer/delegation tool is exposed in this execution session. This is an implementation-author changed-path audit, NOT independent review. Prior review/INDEPENDENT-REVIEW.md remains CHANGES_REQUIRED until a reviewer decides. Changed next approach: hand this frozen candidate and coverage to the parent/independent reviewer; no repeated author repair/review loop.

Changed-path coverage (repair/tests/g7/close-ownership.mjs; 23 scenarios):
- durableWrite cleanup: write-primary-temp, write-temp-only, write-parent-only, write-postpublish, write-undefined. Primary identity (even undefined), first cleanup-only error, all remaining closes; postpublication target remains visible despite error.
- old selection close: write-old-primary/write-old-only. Validation primary beats old/parent close errors; cleanup-only close fails rather than publishes.
- readPrivate file/parent cleanup: read-primary/file-only/parent-only/open/owned.
- parent ownership transfer and cleanup: parent-reuse/next-owned/initial-owned. openPrivateRoot: root-reuse/root-owned. Released FD is reallocated to an unrelated sentinel; sentinel survives final candidate, newly owned next FD closes. Before-mode asserts both original reuse-destruction/next-leak defects.
- listPrivate: list-primary/list-only. listPrivateBounded nested cleanup: bounded-primary/directory-only/parent-only/root-primary. Nested finally already attempted both closes but masked errors; correction preserves first error and still attempts both. Dir.closeSync is modeled AFTER real close.
All modified close sites exercised; this is scenario/path mapping, not instrumented statement/branch coverage or exhaustive syscall-failure certification. Unmodified successful write/read/traversal paths exercised by 18 boundary and 8 bounded-write cases, using final helper. API signatures/exports unchanged. One private cleanup helper only, no publication/rollback protocol changes.

Exact VM finite TS erasure and closed import mapping retained; support archive/validation and published bounded test body verified against exact ordinary git show 061f48aefaa7c1ffb57d66dd13e472eaca4f7ded. No children/network in test closure. Each Node run serial timeout30+5, CPU20, AS2GiB, 64FD, FSIZE64KiB, core0; not aggregate containment. EIO AFTER real Linux close models a released descriptor; NOT hardware/cross-platform, power-loss/crash, native/fullCell qualification.
First before-run exit1 was a harness assumption about lowest-free FD; test v1, log, run record and scratch retained. Corrected harness retains bounded fillers to force exact reuse; no candidate source change. Before exit0 means expected defects reproduced, not original correctness.
Production/recovery/checkpoint/native/NULL/Case05/final gates unchanged. G7 incomplete. No live source/index/ref/commit/push/CURRENT/M4/cron changes. All originals/evidence retained. Minimal publication map is a proposed mapping only; tests are exact candidate-bound VM harnesses, not deployment or approval. Reviewer reserve untouched.
