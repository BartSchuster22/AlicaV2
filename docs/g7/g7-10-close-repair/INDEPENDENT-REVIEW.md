# BLOCKER: repository test binding / duplicate publication
Frozen source 825c1f99cfe0760bd4f3d1c69fe13294ce1d3c962a23b0ceaa99f3634b2f3a40
Base 061f48aefaa7c1ffb57d66dd13e472eaca4f7ded. Not SOURCE_READY as a publication set.

Prior cleanup masking/parent leak: CLOSED. Traversal released-FD reuse/next leak in parent and openPrivateRoot: CLOSED. Reviewed every changed close path against exact git show: write temp/parent, read file/parent, list, bounded directory/parent/root, old selection, traversal and ownership-check failures. Ownership transfers before close; remaining cleanup runs once; primary (including undefined) wins; first cleanup-only error escapes. No public API change or observed owned-FD leak/doubleclose. This is Linux source-component closure, not exhaustive syscall qualification.

Independent serial reruns: 23 close +18 boundary +8 bounded PASS on FINAL hash; commands/hashes/logs in rereview/runs.json. Before-mode rerun reproduces four asserted original defects: write/read primary masked plus one parent leak; both traversals destroy reused sentinel plus leak next. It loads 0c707330 (prior candidate), NOT base; exact base diff is only initial normal-close ownership clearing. Other before results also falsify final-mode good assertions for changed error-precedence paths. These are real FS observations, not harness assertion failures. Bounded fillers force actual same-number reuse; prior failed harness remains retained. EIO follows real Linux close; not real device error. Boundary pre-syscall vs post-success callback semantics retained; publication errors do not imply rollback.

BLOCKER: line7 of ALL THREE frozen tests uses file:///home/alica-dev/g7-owner-ingress-v2/durability-boundary/, choosing repair/tools/g7-durable.mjs. Mapping to tests/g7/g7-10-repair/ does not alter this: repository source is ignored, another machine lacks dependencies. Running from repo cwd passed only via external absolute files, NOT portable publication. bounded.mjs differs from already-published tests/g7/durable-write-faults.test.mjs ONLY in this loader; publishing a duplicate is unjustified.
Closure action (parent only; no further source repair cycle): retain frozen source; make a separate publication-only test-loader/destination correction, bind two new tests to repo-relative source, reuse existing bounded test at its original path, then verify that exact repository-layout set. No code edits here. publication-set.json is a hash-exact BLOCKED handoff, not approval.

Archive/validation match base; finite checked TS erasure preserves code/check/digest; imports audited, no test child/network. Scoped source/tests/docs screen: no credentials or added third-party/license material. ALLOCATION.json measured before writes; caps/reserves retained, no reallocation/refund. Existing Node; serial30+5wall/20CPU/2GiB/64FD/64KiB/core0 per process, not aggregate. No crash/freshCell/device/cross-platform qualification, deployment or publication. G7 incomplete; parent sole publisher.
