# Ordinary runtime assembly source recovery

SOURCE-REVIEWED, UNEXECUTED WIP. Separate author and independent full-patch review completed; independent disposition PUBLISH-SOURCE-WIP. Parent verifies exact patch/base, all fourteen source identities and absence at base, isolated-index application, Python AST and whitespace. Fourteen additions: assembler, six helpers and seven existing focused regression sources. No existing product file replaced.

Direct ordinary helper interface membership is recovered. The assembler explicitly enumerates 16 shipped tool names (author memo incorrectly said 17); those ordinary paths exist at base. This is NOT whole runtime dependency closure, a generated assembly, artifact admission, legal clearance or operational qualification. Python descriptions now distinguish isolated assembly helper invocation from the existing PATH-selected runtime bridge python3 without -I/-B; execution policy is unchanged.

Pending runtime gates:
- Fresh native ownership locations are not threaded through binary consumer and assembler receipt/output/binding/map references. Defaults must not be satisfied by copying excluded artifacts; old expected/installed mismatch remains unblessed.
- Exact native source/output/receipt/compiler/map/Node/age and workspace build inputs require separate admission. No referenced protected payload was read/copied/hashed/loaded/generated in this integration.
- Binary and Node-source consumers still use assert validation, unsafe under optimized Python. Current assembly helper argv does not request optimization; no general optimization-safe API guarantee.
- Local source ceilings/buffering/external calls and post-write aggregate checks do not establish shared failure-inclusive fit; partial output residue remains possible.
- All new regression sources are UNEXECUTED. Some Python suites read real cached/host/protected inputs or spawn tools and are NOT parser-only. Parser source identity matches prior published parser, not a new test pass.

JS test filenames enter the existing G7 wildcard; the push/pull-request foundation workflow invokes native builds and broad checks. SOURCE approval is not test/build authority. This publication uses a [skip ci] commit marker; no workflow dispatch or workflow configuration change is requested. Earlier CI status is not inferred.

Source screen found ordinary project code/tests/constants, no embedded private credentials or protected/vendor payloads. Private/UNLICENSED and third-party notice/legal gates remain. Latest backup/restore/staging/transport fixes are preserved by additions-only integration. Original Case05/NULL/native grants/caps, M1/M2, final signing and owner acceptance remain unchanged/open.
