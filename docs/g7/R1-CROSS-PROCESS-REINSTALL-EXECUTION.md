# R1 cross-process reinstall — execution record

Candidate only; no parent publication, final owner acceptance or G7 completion.

## Scope and preservation

Both initial HEADs were `bd90730511bb20950bd5564cb394055e82105fe8`. The local tree was clean; development contained existing untracked G6/G7 evidence. No competing writer/test was observed at initial preflight. Original regular files enumerated by tracked plus untracked/nonignored Git inventories are bound in `evidence/g7/cross-process-reinstall/{local,development}-before.json`. This is not a whole-filesystem/toolchain inventory. Final comparison and exact source bindings are recorded separately; original manifest evidence and the previously corrected IPC fixture are not edited.

One source writer, no agents/cron, no commits/pushes. SSH used the supplied identity and strict known-hosts checking to development `/home/alica-dev/AlicaV2`; all development commands used `PATH="$PWD/.tools/node/bin:$PATH"`. No dependency/global install, service, RAM/swap, deployment, production signing/key access or protected licensing/archive action occurred.

The executable transfer list is each run's `allowlist.json`; `source.json` binds local transfer inputs, and `input.tar` retains those exact bytes. Final focused/full runs additionally capture development `before.sha256` and `after.sha256`. Returned archives retain tested development source and evidence, with exact member/type/path validation before synchronization. New documentation and evidence are separately preserved. Unrelated development files are not transferred back over local files.

## Original failures and corrections (all retained)

All paths below are under `evidence/g7/cross-process-reinstall/`.

- `focused-1`: the outer tool command timed out after 120 seconds while the remote sequential tests continued. The native build succeeded; all seven cases ultimately failed waiting for CLEAN. Live process inspection showed that the remote run was still executing, so no overlapping retry was started. Final remote failures and artifacts are retained in `focused-1/remote-evidence.tar`; the original input archive is retained. The first compound local archive-extraction command was held by a tool approval gate and did not execute. Evidence was subsequently collected as an archive without that local shell extraction.
- `diagnostic-1`: test-only stack reporting identified `Unauthenticated packet`. A direct Linux socketpair/recvmsg reproduction returned flags `1073741824` (`MSG_CMSG_CLOEXEC`) for a valid credential packet. The native receiver incorrectly treated that echoed input flag as an authentication failure. It now permits only that known flag, still rejects truncation, wrong credentials, empty packets and extra ancillary data, and closes any received rights. This does not relax peer authentication. The evidence-return helper also initially rejected the archive's root directory entry; exact matching of that directory was added, without widening its allowed file prefixes. The diagnostic archive remains intact.
- `focused-2`: 7/7 passed after the credential fix, including exact byte/inode-preserving reinstall and forged/trust/archive/authorization/coordinator-death negatives.
- `upgrade-1`: 7/7 passed for initial distinct-target transaction publication and accepted-selection crash/uncertainty boundaries. Its target changed bundle inventory only; it is not substituted for the later actual provider/profile/lock upgrade coverage.
- `extended-1`: 20 passed, one cancelled on the test's unchanged 30000 ms deadline. The new concurrency test requested mode `pause`, while the hook actually implemented `pause-clean`; its FIFO writer consequently blocked without a reader. The exact test-only FIFO writer was identified from live process state and contained via pidfd; this was not clean-ownership evidence. The mode was corrected and helper teardown registered, without changing any production/test deadline. Real 30-second blocked cleanup and 300-second blocked maintenance passed in this original run.
- `formatted-target-1`: 10/15 passed. Three negative upgrade cases had no diagnostic because closing the private channel raced watchdog SIGKILL against CLI NEEDS_OPERATOR output; final fault residues showed prior preservation, including STAGING on grant-admission failure and ABORTED on readiness/call failure. The failed relationship now retains its channel/pidfd and original independent watchdog deadline; no deadline is extended and no success/reap is minted. The failed owned Cell cannot close to release its original lock. The CLI can report NEEDS_OPERATOR before the unchanged watchdog containment deadline. Two new commit-boundary assertions also misread unpublished `next-*` temporary bytes as committed journal rows. The test now separately validates published numeric records and explicitly asserts retained temporary residue, its COMMITTED payload and the post-link hardlink count; production still rejects residue. The grant-admission case now asserts the actual conservative STAGING state, not an invented ABORTED transition. No existing fixture or baseline expectation was weakened.
- `correction-2`: 11/11 passed, covering the three target negatives and eight publication/commit crash/uncertainty cases.
- `focused-final`: 92/93 passed, with no cancellation or skip. A strengthened replay assertion exposed a missing `--experimental-vm-modules` flag in the NEW subprocess refusal tests: the subprocess could fail at module import rather than proving custody/fence rejection. All such invocations in the new tests were corrected; constructor checks now require the actual CONFLICT/FAILED_PRECONDITION error, CLI replay requires NEEDS_OPERATOR, and concurrent CLI calls require the exact NEEDS_OPERATOR/CONFLICT object. Existing ownership and IPC fixtures were not changed. This was a harness coverage defect, not accepted as a passing refusal.
- `refusal-correction`: 2/3 passed; the new strict concurrent-CLI assertion mistakenly expected a nested error object. It was corrected to the existing CLI's exact flat `{status,code}` response, with neither an alternative error allowance nor a deadline change.
- `refusal-correction-2`: all three targeted strict-refusal checks passed.
- `format-final`: exact source formatting, Python syntax and `git diff --check` succeeded. Historical evidence was outside formatting scope. The new refusal-test-only changes were subsequently formatted before `focused-final-2`.

## Actual coverage and limits

The new successful path is not a read-only inspector relabelled as reinstall: it enters the existing exact-reinstall implementation from the authenticated coordinator lifecycle and invokes the public Kernel on a verbatim private copy of original opaque high-water bytes. All original Cell regular-file byte/hash/inode/mode/mtime bindings are compared across the no-op boundary. The original accepted archive is retained separately even in archive-tampering negatives.

The later upgrade target changes actual provider code/version, profile/version, package index, lock and authorization/profile binding under disposable test signing. Target-only offline lock preparation does not supply replacement Cell/Kernel state to production upgrade. The real mutating path continues using original Cell identity and Kernel state. Original immutable release and transaction bytes/inodes remain unchanged, while the new revision is admitted, activated through real IPC and durably published only after provider/consumer synthetic calls.

Adversarial coverage includes actual anonymous Unix credentials from separate child/grandchild processes, pidfd lifetime, wrong sender/rights/truncation/empty packets, caller-supplied clean/receipt/PID objects, replayed invocation, forged selection, revoked/equivocating current trust, changed authorization/archive, continuous separate flock competition and concurrent constructor attempts. Death, actual synchronous FIFO-blocked verification/activation/cleanup, a genuine buffered CLEAN returned after the real cleanup deadline, accepted-selection rename/fsync and transaction-commit uncertainty are separately exercised. Existing owned-coordinator sealing/admin tests remain necessary for shared v1/v2 admission and exact original owner/custodian reap coverage.

Success does not retire the supervision fence or permit an ordinary constructor/restart. Uncertain commit retains the observed selection and journals; no rollback/replay or automatic prior revival occurs. A visible COMMITTED row after a pre-fsync crash is not claimed durable acceptance. Process/SIGKILL tests are not physical power-loss tests, and same-UID tests do not qualify distinct-UID denial. Full serving succession and uncertain-crash recovery remain incomplete. Distribution/SBOM, approved encrypted backup/restore, independent online/disconnected two-host qualification, owner signing and final acceptance remain outstanding as described in the companion checkpoint.

## Final qualification

Final raw results and exit codes are retained in `evidence/g7/cross-process-reinstall/focused-final-2/` and `full-final/`. `evidence/g7/cross-process-reinstall/verification.json` records actual per-suite totals, source-before/after bindings, local/development synchronization, original-inventory comparisons and unchanged historical-evidence counts. A failed original run is not overwritten by a later pass. The delivery manifest binds the resulting files but grants no custody permission. Parent must independently review/rerun/publish; this worker does not accept G7.
