# Exact reinstall — independent parent review

Baseline 0ccc6048660cc3f3330588c2e979a37a7edc763e. The independent full development regression completed with exit 0: Node 170/170, 160/160, 228/228 and Python 193/193, 30/30, 11/11, 121/121. Four before/after/local source/doc hashes match. All 67 worker evidence-manifest entries independently verified. Separate parent logs and source binding: evidence/g7/exact-reinstall-parent. No manifest-bound worker evidence was rewritten.

Reviewed implementation: exact reinstall on the SAME continuously locked object following confirmed clean public Kernel shutdown. Both retained release and requested archive are verified with current trust and accepted bindings before any transaction. Opaque Kernel continuity is checked through public bootstrap on a private verbatim copy, never by rewriting original high-water bytes. Shutdown uncertainty retains custody; reopened Cells cannot reconstruct a clean permit from residue. Real tests assert repeated byte/inode/mode/mtime preservation and reject artifact, authorization, trust, journal and state corruption. This is a bounded component capability, not offline handoff or production upgrade.

The lifecycle completion checkpoint/execution records remain the detailed coverage and limitations record; their publication-pending wording describes the worker handoff. Publication does not qualify standalone installation, upgrade, post-crash reconciliation, backup/restore or two-host acceptance. No production signing or G7 acceptance occurred.

Next authorized implementation direction is a coordinator that owns its custodian FROM INCEPTION, retains the same flock, seals admission and proves clean Kernel shutdown plus exact owner/custodian reaps before an offline operation. This must be implemented and adversarially qualified; it is not delivered here. Existing detached custodians cannot be taken over or released merely because stop returned. Existing public admin schema/stop/start semantics remain unchanged; adding a live handoff operation would be a separate decision, not assumed authority.
