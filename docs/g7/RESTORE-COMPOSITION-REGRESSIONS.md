# Ordinary restore composition regression sources

Source-reviewed UNEXECUTED WIP. Parent read the full two-test patch and actual coordinator lifecycle/closure. Static hashes, Python AST and isolated-index application/whitespace verified; author also reported JS syntax-only check. No module import, behavioral test, fixture crypto or native execution was performed.

Python suite imports the full ordinary coordinator/supervisor decoder. Synthetic credential payloads exercise receive/selection checks; malformed closed RESTORE shapes, digest/sequence/frame failures and GO controls are specified. A recording supervisor plus process/protocol doubles drive the actual lexical spawn_owner override at initial and changed serving-successor boundaries, expecting the original input digest in both. This is not a real fork/reap/authentication provenance test.

JS suite evaluates the complete bridge with a closed VM import map. It specifies missing preparation, nonempty/same-inode destination denials; prepare-before-RESTORE and prepared-input forwarding; failure retention; expired callback scope and sticky UNCERTAIN despite a later synthetic zero exit. Native returns, locks and packet origin are doubles. Cell preparation is a fixture: no real snapshot digest computation, trust recertification, accepted-last publication or Kernel cleanup is covered. Existing real qualification suites are unchanged.

Future commands require separate execution admission from a clean exact tree:

    python3 -B tests/g7/restore-composition-ordinary.py -v
    node --experimental-vm-modules --test tests/g7/restore-composition-bridge.test.mjs

The JS filename joins the existing G7 wildcard suite (which separately builds native code); that suite is NOT authorized by adding this source. No command was executed. No historical PASS transferred. No new package/dependency or real credential material was added. G7-13/15/20, WP3/WP6 qualification, exact native artifact admission, failure-inclusive resource fit and original loss/reap tests remain OPEN.
