# Bootstrap SOURCE integration -- unexecuted WIP

Adds development inventory-pinned bootstrap, independent-root signed bootstrap and two Python regression sources. Full independent original review found B1 unsupported admin silent no-op and B2 unsafe-ancestor pathname rebinding. Parent independently read both complete repair deltas and accepts their SOURCE correction; HOLD removed for source publication only. Exact patch hash: 84e13971b21fbe3906c74f492054bf862643ebd93665d7dd4e9efe6561de78a7.

B1: only prepare and owned are launch operations. Unsupported admin explicitly rejects before candidate/operator work. adminRequest library unchanged; this does NOT deliver an installed admin CLI or complete operator guide.

B2: common descriptor-relative no-follow traversal checks root, every intermediate and final directory owner (operator or trusted OS root), directory type and no group/other writes. Candidate/immediate extraction parent keep stricter operator-owned 0700 requirements. Shared writable ancestors INCLUDING sticky /tmp reject. Destination must be named under a single normalized root. Supported safe-root routes retained. FD ownership transferred before closing prior FD; Linux close assumptions apply. Trusted operator/root mutation, privileged mount changes and kernel containment are outside this source claim. Development Node invocation now matches signed --no-global-search-paths; this does not prove full dependency closure.

Signed transport retains independent root pin, operator trust/floor, signature domains, publisher IPC authority, current-clock/revocation checks, policy lock/snapshots and exact inventory/extraction checks. Independently authenticated scripts, sibling schemas and prepared cryptography are prerequisite delivery inputs; repository tools layout is not installed layout. No automatic downgrade/fetch/install. Operator floor is NOT durable Cell rotation. Extraction residue retained on failure. Signed transport is NOT Cell/native admission or SBOM/legal completeness.

Regression SOURCES include unsupported dispatch, unsafe ancestry at each depth, descriptor cleanup contracts, strict final-directory policy, safe-root launch argv and existing signed transport checks. New OS metadata doubles are not rename-race/kernel evidence. Existing real-fixture source now requires safe home ancestry instead of shared /tmp; full signed suite would generate disposable cryptographic fixtures and requires separate authority. NO tests/imports/crypto/fixtures/builds executed. AST, hash, isolated patch applicability and whitespace checks only; no historical PASS transferred.

Publication [skip ci]. No protected artifact/native/compiler/receipt/map/archive/license payload reads, copies, hashes or probes; no consumed grant renewal/cap change. Independent-host operation, exact-source behavioral tests, native/ABI/confinement/dependency admission, enforcement/failure-inclusive fit, provenance/legal/signing/release and explicit owner acceptance remain OPEN. G7 not complete.
