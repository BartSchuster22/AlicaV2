# Restore composition source checkpoint

UNEXECUTED WIP. Parent independently reviewed the full three-path selective patch, including Cell validation/publication order, bridge protected destination scope, coordinator authenticated RESTORE selection and both owner launches. Static patch application, hashes, whitespace and Python AST verified. Author also reported successful JS syntax-only checks; no imports, native loads or product tests occurred.

This composition retains the original restore stage/construct/run paths and source/current-authority/inventory checks. The runtime path prepares a distinct empty locked destination, verifies materialized bytes and opaque Kernel shutdown, advances floors, publishes accepted.json last, then sends the digest of the same validated runtime input snapshot. Both initial restored and normally reaped serving-successor launches forward that binding to the already integrated supervisor/owner. Static connection is not observed end-to-end success.

Mandatory destination preparation has no initialized-root fallback. Private startOwned selection is not an exported arbitrary-selection API. Sticky UNCERTAIN and retained failure custody remain; no timeout/PID absence/receipt replaces exact normal reap.

Pending runtime gate: exact native artifact/interface providing armChildContainment/endChildContainment plus original source-watch-loss/destination-lock/exact-reap/sticky-uncertainty behavior must be separately admitted and qualified before use. Comments/documentation are governance labels, not a runtime kill switch. No native artifact/source was inspected/copied/executed/admitted. Existing native references were preserved, not replaced with mock successes.

Excluded: ownedBackup capture, readiness/application diagnostics, owner FD lifetime edits, unrelated archive limits/interpreter invocation, all native/NULL/setup changes. Latest transport stdout fixes and owner/supervisor binding remain unchanged. No new dependencies or literal secrets found in selected source review; external licensing/notices remain separate.

G7-13/15/20 and WP3 remain OPEN pending exact-composition tests, both-launch mutation denials, positive current-authority/fresh-call/teardown evidence, native custody/loss qualification and failure-inclusive resource admission. Historical tests of other bytes do not transfer. Next source step is focused regression coverage for the newly composed ordinary interfaces; no execution grant is inferred.
