# Step-1 authorization

The owner instructed: “start with Step-1 implementation. Every phase commit and push to https://github.com/BartSchuster22/AlicaV2”. The owner reiterated that ENTRY prerequisites must be satisfied and EXIT needs evidence, not intent/scaffolding.

This authorizes execution of the proposed Step-1 plan and documentation/design work. It does not erase the explicit G1 normative-acceptance requirement. No self-approval of normative specifications is inferred.

Scope: new Kernel, versioned ACAP, public SDK, synthetic providers, IPC and standalone packaging qualification. Higher-level service integration, production data migration and changes to V1/protected licensing artifacts remain excluded.

The distribution-only reference boundary documented in the approved plan applies. Missing private-source internals/human owners stay UNVERIFIED, not silently complete. Additional concept documents were not supplied; this implementation proceeds from the provided plan/directive, recording unresolved semantics as design decisions.

Per-phase commits and pushes are required. No force pushes or weakening protected branches. A tool/resource limit must be reported with completed work and outstanding gates. Administrative host changes remain separately bounded by least privilege.
