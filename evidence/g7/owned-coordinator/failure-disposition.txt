Raw failure disposition (no original log/source archive overwritten)

1. focused-2.log/.exit: a coordinator SIGKILL test paused before SEALED expected every live Cell file to be unchanged. The actual diff is kernel.json (hash/inode/mtime), not accepted.json, identity, G7 floor, releases or transaction journals. The public Kernel had not shut down/reaped yet and was still allowed to persist its opaque high-water observations. The crash test now records both snapshots and excludes only kernel.json/kernel.json.next at explicitly live pre-reap boundaries. It does not reset, parse or discard those files. All-file no-op assertions after actual clean reaps remain strict.

2. final-qualification/focused-final.log and seal-diagnostic.log: the concurrent v1/v2 seal test had the same invalid live-Kernel no-op assumption at after-SEALED, before stop/reap. A targeted TAP diagnostic reproduced only a kernel.json delta. The final test retains the two snapshot bindings, denies all racing admin responses, requires unchanged immutable selection/journals/releases/identity/G7 floor, and requires exactly one owner spawn plus actual successful stopped verification. Post-reap all-file preservation remains independently strict.

3. full-check.log/.exit: an unchanged G6 IPC same-source matrix consumer test returned UNAVAILABLE where DEADLINE_EXCEEDED was asserted. ipc-failure-source.sha256 binds its unchanged sources. ipc-failure-focused.log/.exit records the subsequent isolated diagnostic run without source, deadlines, resources or test changes. The underlying cause is NOT established. No G6 deadline/expectation was loosened. A later whole-run result must be read separately; an isolated pass alone is not full-regression success.

4. local-channel-final.log/.exit and local-channel-failure-input.tar: the newly added unit test passed pid and pidfd in reversed positional order to until(). The observed error was select() filedescriptor out of range. The test call was corrected; production until() signature/semantics were not relaxed. local-channel-final-2.log records the corrected run.

5. local-channel-final-3.log/.exit and local-channel-failure-3-input.tar: the added parent-death test called a nonexistent guard helper name. Its fork child consequently emitted an AttributeError and the parent observed empty PID bytes. The test was corrected to call the actual guard_parent(); local-channel-final-4.log records the real process/held-pidfd test passing. No production guarding was bypassed.

Final source review also added fresh elapsed-time checks after authenticated receive and exact waitpid return, and anchored budgets before GO and before offline worker creation. These prevent late syscall/launch returns from getting a fresh interval. Helper tests use short real elapsed deadlines to target post-return checks; they are not substituted for the separate real unchanged 30-second/300-second FIFO qualifications.
